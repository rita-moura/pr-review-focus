{
  "name": "github-pr-code-only",
  "version": "0.6.1",
  "private": true,
  "description": "Chrome extension for focused GitHub pull request diffs.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check rules.js && node --check dom.js && node --check patch.js && node --check content.js && node --check background.js && node --check diff.js"
  },
  "engines": {
    "node": ">=18"
  }
}
