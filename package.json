{
  "name": "pr-review-focus",
  "version": "0.8.0",
  "private": true,
  "description": "Chrome extension for focused GitHub pull request diffs.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:browser": "node tests/browser-smoke.cjs",
    "check": "node --check rules.js && node --check dom.js && node --check content.js && node --check background.js && node --check diff.js && eslint ."
  },
  "engines": {
    "node": ">=20.19"
  },
  "devDependencies": {
    "eslint": "^10.10.0",
    "playwright-core": "^1.63.0"
  }
}
