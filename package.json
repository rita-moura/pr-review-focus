{
  "description": "Chrome extension for focused GitHub pull request diffs.",
  "engines": {
    "node": ">=18"
  },
  "name": "github-pr-code-only",
  "private": true,
  "scripts": {
    "check": "node --check rules.js && node --check dom.js && node --check content.js",
    "test": "node --test tests/*.test.cjs"
  },
  "version": "0.1.4"
}
